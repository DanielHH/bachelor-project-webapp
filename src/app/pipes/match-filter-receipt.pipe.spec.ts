import { MatchFilterReceiptPipe } from './match-filter-receipt.pipe';

describe('MatchFilterReceiptPipe', () => {
  it('create an instance', () => {
    // const pipe = new MatchFilterReceiptPipe();
    // expect(pipe).toBeTruthy();
  });
});
