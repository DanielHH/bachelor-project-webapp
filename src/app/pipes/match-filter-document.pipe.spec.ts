import { MatchFilterDocumentPipe } from './match-filter-document.pipe';

describe('MatchFilterDocumentPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchFilterDocumentPipe();
    expect(pipe).toBeTruthy();
  });
});
