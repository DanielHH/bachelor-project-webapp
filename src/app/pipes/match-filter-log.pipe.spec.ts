import { MatchFilterLogPipe } from './match-filter-log.pipe';

describe('MatchFilterLogPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchFilterLogPipe();
    expect(pipe).toBeTruthy();
  });
});
