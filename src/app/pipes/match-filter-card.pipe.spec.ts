import { MatchFilterCardPipe } from './match-filter-card.pipe';

describe('MatchFilterPipe', () => {
  it('create an instance', () => {
    /* const pipe = new MatchFilterCardPipe();
    expect(pipe).toBeTruthy(); */
  });
});
